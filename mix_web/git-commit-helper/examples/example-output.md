# 示例 1：仅生成提交消息

📝 docs: 补充 Git 提交消息生成规范

新增基于 `git diff` 生成中文提交消息的固定输出格式。
补充类型判断规则与标题长度限制，保证提交信息风格统一且可直接复用。

# 示例 2：生成消息并提交

🔧 chore: 忽略仓库中的系统缓存文件

新增 `.gitignore` 规则，避免 `.DS_Store` 一类系统文件进入版本控制。
减少无关噪音文件对工作区和提交记录的干扰，保持仓库历史更干净。
